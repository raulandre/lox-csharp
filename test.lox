fun fib(n)
{
    if(n <= 1) return n;
    return fib(n-1) + fib(n-2);
}

fun fact(n)
{
    if(n == 0) return 1;
    return n * fact(n-1);
}

print("Enter a number:");
var n = number(read());

print("Fib: " + fib(n));
print("Fact: " + fact(n));
exit(0);
